import { Navbar, HeroSection, ToolsCarousel, ServicesSection } from '@/components';

export default function Home() {
  return (
    <main>
      <Navbar />
      <HeroSection />
      <ToolsCarousel />
      <ServicesSection />
    </main>
  );
}